{
    "compilerOptions": {
      "target": "es5",
      "lib": ["es5", "dom"],
      "types": ["cypress", "node"],
      "noEmit": true,
      // be explicit about types included
      // to avoid clashing with Jest types
    },
    "include": ["**/*.ts"],
  }